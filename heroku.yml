setup:
  addons:
    - plan: heroku-postgresql
      as: db
build:
  docker:
    server: server/Dockerfile
    web: client/Dockerfile
run:
  server: npm run start
release:
  image: server
  command:
    - ./reboot_db.sh
